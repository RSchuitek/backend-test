using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace PBBackend.Test.UnitTests.Controllers
{
     [TestClass]
     public class PessoaControllerTest
     {
          // Criar mock de PessoaRepository

          public PessoaControllerTest()
          {
               // Criar instancias para ser utilizadas nos metodos 
               // Criar objetos fack 
          }

          [TestMethod]
          public void PessoaController_SalvarPessoa_ReturnoComErro()
          {
               
          }

           [TestMethod]
          public void PessoaController_SalvarPessoa_ReturnoComSucesso()
          {
               
          }

          // Verificar campos obrigatorios no método de salvar 
          // Verificar campos com valores maximos no método de salvar 
          // Verificar campos com valores minimos e nulos no método de salvar 

          // Verificar Metodo para alterar 
          // Verificar campos obrigatorios no método de alterar 
          // Verificar campos com valores maximos no método de alterar 
          // Verificar campos com valores minimos e nulos no método de alterar 

          // Verificar Metodo para excluir - retorno com sucesso 
          // Verificar Metodo para excluir - retorno com sucesso 
     }
}